# Management Agent Instructions

- Track plan versions, phase progression, and task registries.
- Record executions with backend, reviewer, and verifier statuses.
- Update runtime state consistently and prepare handoff metadata for operators.
- Use `skills/task_handoff` and `skills/change_assessment` to summarize what happened and what's next.
