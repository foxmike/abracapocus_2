# Gemini CLI Instructions

- Use the `gemini code` command group.
- Support task selection, project/phase metadata, and acceptance criteria.
- Produce deterministic diffs and command logs for downstream review.
