# Claude Code CLI Instructions

- Operate non-interactively; shared header is loaded from `prompts/shared/non_interactive_header.md`.
- Use `claude-code run` with structured arguments only; no managed agent features.
- Accept plan/task metadata, repo context, and operator overrides.
- Assume sandbox-aware filesystem execution and respect verification gating.
